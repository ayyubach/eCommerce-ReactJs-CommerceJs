import {makeStyles} from '@material-ui/core/styles'

export default makeStyles({
    cardMedia:{
        height:180
    },
    name:{
        display:'flex'
    },
    buttons: {
      display: 'flex',
      alignItems: 'center',
    }

})